document.addEventListener('DOMContentLoaded', () => {
    const EmailButton = document.getElementById('btn');
    
    EmailButton.addEventListener('click', () => {
      window.location.href = 'mailto:arerepadelouisbenagha@outlook.com';
    });
});
